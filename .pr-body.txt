## Summary

**Issue:** File names from video titles may contain characters illegal on Android (/:|* or quote chars), causing downloads to silently fail. The quality selection bottom sheet also showed a "clear" icon as placeholder.

**Changes:**
- `DownloadWorker.kt`: Added `sanitizeFileName()` — strips illegal FS chars, limits to 80 chars
- `MainScreen.kt`: Replaced placeholder `Icons.Default.Clear` with `Icons.Default.Download` in quality selection list

**Testing:** Local build passes; CI (GitHub Actions) runs unit tests on every push.

---
_Generated by Jules (automated PR)_
